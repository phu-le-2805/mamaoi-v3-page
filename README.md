# Mommi Memory — landing page

Next.js (App Router). Trang: `/` giới thiệu, `/contact`, `/privacy`, `/terms`.

```bash
npm install
npm run dev
```

Thông tin liên hệ, ngày hiệu lực, khu vực máy chủ: sửa trong `lib/site.ts`. Deploy: import repo vào Vercel (không cần cấu hình thêm).
